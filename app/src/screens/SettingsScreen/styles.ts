import { ScrollView, View } from "react-native"
import { styled } from "../../hooks/theme"

export const Scroll = styled(ScrollView)`
    padding: 16px 0px;
`

export const PaddingHorizontalContainer = styled(View)`
    padding: 0px 16px;
`